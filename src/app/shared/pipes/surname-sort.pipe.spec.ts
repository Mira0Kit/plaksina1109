import { SurnameSortPipe } from './surname-sort.pipe';

describe('SurnameSortPipe', () => {
  it('create an instance', () => {
    const pipe = new SurnameSortPipe();
    expect(pipe).toBeTruthy();
  });
});
